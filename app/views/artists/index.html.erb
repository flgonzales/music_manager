<p id="notice"><%= notice %></p>

<h1>Listing Artists</h1>

<% @artists.each do |artist| %>
  <p><%= link_to artist.name, artist %></p>
  <p><img class="small-cover" src=<%=artist.avatar %></p>
  <p><%= link_to artist.last_update %></p>
<% end %>

<!--
<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th>Avatar</th>
      <th>Last update</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @artists.each do |artist| %>
      <tr>
        <td><%= artist.id %></td>
        <td><%= artist.name %></td>
        <td><img class="small-cover" src=<%= artist.avatar %>></td>
        <td><%= artist.last_update %></td>
        <td><%= link_to 'Show', artist %></td>
        <td><%= link_to 'Edit', edit_artist_path(artist) %></td>
        <td><%= link_to 'Destroy', artist, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
-->
<br>

<%= link_to 'New Artist', new_artist_path %>
